---
title: "Enviar Áudio"
icon: "microphone"
api: "POST /send_audio"
---

<ParamField header="x-api-key" type="string" required placeholder="SUA_SENHA_SECRETA">
  Chave de autenticação da API.
</ParamField>

Envia um áudio. Se for menor que 15MB, envia como áudio padrão (ou PTT). Se for maior, envia como documento.

### Corpo da Requisição (Body)

<ParamField body="phone" type="string" required>
  Número do destinatário com código do país.
</ParamField>

<ParamField body="audio_url" type="string" required>
  URL pública direta do arquivo de áudio (mp3, wav, ogg).
</ParamField>

<ParamField body="ptt" default="false" type="boolean">
  Se `true`, aparece como mensagem de voz (apenas para arquivos pequenos).
</ParamField>

<ParamField body="asDocument" default="false" type="boolean">
  Força o envio como arquivo.
</ParamField>


<ResponseExample>

```json 200 Sucesso
{
  "success": true,
  "message": "Áudio enviado com sucesso ✅"
}
```

```json 401 Erro
{
  "error": "Unauthorized",
  "message": "API Key inválida ou ausente"
}
```

</ResponseExample>