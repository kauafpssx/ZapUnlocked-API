---
title: "Otimização de Recursos"
icon: "server"
description: "Saiba como a ZapUnlocked-API utiliza o mínimo de recursos para rodar em qualquer ambiente."
---

A **ZapUnlocked-API** foi projetada para ser extremamente leve, rodando de forma estável em ambientes com recursos limitados como o plano gratuito da **Alwaysdata** (256MB de RAM).

---

## <Icon icon="server" iconType="sharp-regular" color="#9d4fe6" size={22} /> Consumo de Recursos

<div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "16px", margin: "24px 0" }}>
  <div style={{
    background: "linear-gradient(135deg, #4c1d95 0%, #7c3aed 100%)",
    borderRadius: "16px",
    padding: "28px",
    textAlign: "center"
  }}>
    <div style={{ color: "#e9d5ff", fontSize: "12px", fontWeight: 600, letterSpacing: "1.5px", textTransform: "uppercase", marginBottom: "16px" }}>
      Em repouso (idle)
    </div>
    <div style={{ display: "flex", justifyContent: "space-around" }}>
      <div>
        <div style={{ color: "#ffffff", fontSize: "32px", fontWeight: 800, lineHeight: 1 }}>~140MB</div>
        <div style={{ color: "#c4b5fd", fontSize: "12px", marginTop: "4px" }}>RAM</div>
      </div>
      <div>
        <div style={{ color: "#ffffff", fontSize: "32px", fontWeight: 800, lineHeight: 1 }}>&lt;1%</div>
        <div style={{ color: "#c4b5fd", fontSize: "12px", marginTop: "4px" }}>CPU</div>
      </div>
    </div>
  </div>
  <div style={{
    background: "linear-gradient(135deg, #7c3aed 0%, #4c1d95 100%)",
    borderRadius: "16px",
    padding: "28px",
    textAlign: "center"
  }}>
    <div style={{ color: "#e9d5ff", fontSize: "12px", fontWeight: 600, letterSpacing: "1.5px", textTransform: "uppercase", marginBottom: "16px" }}>
      Em operação (mídia/botões)
    </div>
    <div style={{ display: "flex", justifyContent: "space-around" }}>
      <div>
        <div style={{ color: "#ffffff", fontSize: "32px", fontWeight: 800, lineHeight: 1 }}>~220MB</div>
        <div style={{ color: "#c4b5fd", fontSize: "12px", marginTop: "4px" }}>RAM</div>
      </div>
      <div>
        <div style={{ color: "#ffffff", fontSize: "32px", fontWeight: 800, lineHeight: 1 }}>5-15%</div>
        <div style={{ color: "#c4b5fd", fontSize: "12px", marginTop: "4px" }}>CPU</div>
      </div>
    </div>
  </div>
</div>

---

## <Icon icon="cloud" iconType="sharp-regular" color="#9d4fe6" size={22} /> Compatibilidade com Alwaysdata (Plano Free)

<CardGroup cols={2}>
  <Card title="RAM: 256 MB" icon="microchip">
    Consumo em repouso de ~140MB deixa margem confortável. Picos em operação ficam em ~220MB.
  </Card>
  <Card title="CPU: 0.25 vCPU" icon="gauge-high">
    Otimizado para uso fracionado. Picos ocorrem apenas durante conversão de áudio ou imagem.
  </Card>
  <Card title="Armazenamento: 1 GB SSD" icon="hard-drive">
    Generoso para uso normal. Compressão gzip reduz histórico em até 70%.
  </Card>
  <Card title="Backups: 3 dias" icon="clock-rotate-left">
    Retenção automática de 3 dias pela Alwaysdata. Sessão de auth fica em arquivo local.
  </Card>
</CardGroup>

<Warning>
  No plano gratuito da Alwaysdata, processos iniciados via SSH são finalizados após inatividade. Para manter a API ligada 24/7, cadastre a execução em **Web > Services** no painel da Alwaysdata.
</Warning>

---

## <Icon icon="circle-check" iconType="sharp-regular" color="#9d4fe6" size={22} /> Boas Práticas

- **Limpeza de histórico**: use `/management/cleanup` regularmente para evitar acúmulo de dados em disco
- **Mídias pesadas**: a fila de processamento garante que apenas um arquivo seja processado por vez, sem picos de memória
- **Log em produção**: mantenha o log em modo normal para evitar escrita excessiva em disco

<Warning>
  Se o consumo de RAM exceder o limite do host, o processo pode ser finalizado pelo OOM Killer do sistema operacional. A arquitetura em Python com Neonize é significativamente mais eficiente que implementações em Node.js.
</Warning>
