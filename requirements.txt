# ============================================================
# ZapUnlocked API — Dependencies
# Installation: pip install -r requirements.txt
# ============================================================

# ── Web Server ────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
python-dotenv>=1.0.0

# ── WhatsApp Client (Neonize) ───────────────────────────
neonize>=0.3.18.post0

# ── Media Processing ──────────────────────────────────────
Pillow>=11.0.0
python-magic>=0.4.14; sys_platform != "win32"    # MIME detection — Linux/macOS (needs system libmagic)
python-magic-bin>=0.4.14; sys_platform == "win32" # MIME detection — Windows (bundles libmagic.dll)
imageio-ffmpeg>=0.5.1              # Bundled ffmpeg binary (no manual install)

# ── HTTP Requests ─────────────────────────────────────────
requests>=2.32.0
httpx>=0.28.0

# ── Utilities ─────────────────────────────────────────────
qrcode>=8.0
loguru>=0.7.0
psutil>=7.0.0
aiofiles>=24.1.0
python-multipart>=0.0.20
tzdata>=2024.1                     # IANA timezone database (required for zoneinfo on Windows)
