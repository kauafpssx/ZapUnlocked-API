---
title: 'Enviar Sticker'
icon: 'note-sticky'
api: "POST /send_sticker"
---

<ParamField header="x-api-key" type="string" required placeholder="SUA_CHAVE_SECRETA">
  Chave de autenticação da API.
</ParamField>

Envia um sticker a partir de uma URL de imagem.

### Corpo da Requisição

<ParamField body="phone" type="string" required>
  Número do contato.
</ParamField>

<ParamField body="image_url" type="string" required>
  URL pública da imagem a converter em sticker.
</ParamField>

<ParamField body="pack" type="string">
  Nome do pacote de stickers (ex: "Meu Pacote").
</ParamField>

<ParamField body="author" type="string">
  Nome do autor do sticker (ex: "Bot").
</ParamField>

<ParamField body="resizeMode" type="string" default="pad">
  Modo de redimensionamento da imagem.
  
  | Modo | O que faz | Parâmetro extra |
  | :--- | :--- | :--- |
  | `pad` | Adiciona bordas para preencher (padrão). | `padColor` |
  | `cover` | Corta a imagem para preencher o espaço. | - |
  | `stretch` | Estica a imagem (pode distorcer). | - |
  | `blur` | Preenche o fundo com versão desfocada. | `blurIntensity` |
  | `transparent` | Redimensiona mantendo proporção com fundo transparente. | - |
</ParamField>

<ParamField body="padColor" type="string" default="black">
  Cor de fundo (somente para `pad`). Ex: `#FFFFFF` ou `transparent`.
</ParamField>

<ParamField body="blurIntensity" type="number" default="20">
  Intensidade do desfoque (somente para `blur`). 1 a 100.
</ParamField>


<ResponseExample>

```json 200 Sucesso
{
  "success": true,
  "message": "Sticker sent successfully."
}
```

```json 500 Erro interno
{
  "error": "Erro ao converter imagem"
}
```

</ResponseExample>
