---
title: "Renomear Sessão"
icon: "pen"
api: "PUT /sessions/{session_id}/rename"
---

<ParamField header="x-api-key" type="string" required placeholder="SUA_CHAVE_SECRETA">
  Chave de autenticação da API.
</ParamField>

<ParamField header="session_id" type="string" required>
  ID da sessão a ser renomeada.
</ParamField>

Renomeia uma sessão WhatsApp existente.

### Corpo da Requisição

<ParamField body="name" type="string" required>
  Novo nome para a sessão.
</ParamField>

<ResponseExample>

```json 200 Sucesso
{
  "success": true,
  "message": "Session renamed to: Novo Nome"
}
```

```json 401 Não autorizado
{
  "error": "Unauthorized",
  "message": "Invalid or missing API key. Send via x-api-key header (preferred) or ?API_KEY= query param."
}
```

</ResponseExample>
