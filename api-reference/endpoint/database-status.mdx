---
title: "Status da Limpeza"
icon: "chart-simple"
api: "GET /management/database/status"
description: "Retorna informações da última limpeza e contagem regressiva para a próxima execução."
---


<ResponseField name="last_run" type="number">
  Timestamp Unix da última limpeza.
</ResponseField>

<ResponseField name="next_run" type="number">
  Timestamp Unix esperado para a próxima limpeza.
</ResponseField>

<ResponseField name="interval" type="number">
  Intervalo configurado atualmente (em minutos).
</ResponseField>

<ResponseField name="time_remaining_human" type="string">
  Tempo restante formatado (ex: "23h 59m").
</ResponseField>

<ResponseExample>

```json 200 Sucesso
{
  "last_run": 1708970000,
  "next_run": 1709056400,
  "interval": 1440,
  "time_remaining_seconds": 86400,
  "time_remaining_human": "1440m 0s"
}
```

</ResponseExample>
