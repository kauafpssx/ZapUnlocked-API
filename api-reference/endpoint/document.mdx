---
title: "Enviar Arquivo"
icon: "file-import"
api: "POST /send_document"
---

<ParamField header="x-api-key" type="string" required placeholder="SUA_CHAVE_SECRETA">
  Chave de autenticação da API.
</ParamField>

Envia qualquer tipo de arquivo (PDF, DOCX, ZIP, etc.). Aceita URL pública ou upload direto via `multipart/form-data`. Limite de 400 MB.

### Corpo da Requisição (JSON ou multipart/form-data)

<ParamField body="phone" type="string" required>
  Número do destinatário com código do país.
</ParamField>

<ParamField body="url" type="string">
  URL pública do arquivo. Se omitido, use `file` para upload direto.
</ParamField>

<ParamField body="file" type="file">
  Arquivo via multipart/form-data. Alternativa a `url`.
</ParamField>

<ParamField body="fileName" type="string">
  Nome personalizado exibido no WhatsApp (ex: `documento_importante.pdf`).
</ParamField>

<ParamField body="caption" type="string">
  Legenda exibida junto ao arquivo.
</ParamField>

<ParamField body="reply" type="string">
  ID ou texto da mensagem para responder.
</ParamField>

<ParamField body="quoted_id" type="string">
  ID da mensagem a citar diretamente.
</ParamField>


<ResponseExample>
```json 200 Sucesso
{
  "success": true,
  "message": "Document sent successfully."
}
```

```json 401 Não autorizado
{
  "error": "Unauthorized",
  "message": "Invalid or missing API key. Send via x-api-key header (preferred) or ?API_KEY= query param."
}
```
</ResponseExample>
